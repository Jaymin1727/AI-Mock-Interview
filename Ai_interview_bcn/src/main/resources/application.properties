# ===================================================
# Spring Boot Application Properties
# Values are loaded from the .env file via dotenv-java
# ===================================================

spring.application.name=ai-interview-backend

# Server
server.port=${SERVER_PORT:8080}

# Firebase
firebase.project-id=${FIREBASE_PROJECT_ID:ai-interview-5d5c5}
firebase.api-key=${FIREBASE_API_KEY:}
firebase.service-account-path=${FIREBASE_SERVICE_ACCOUNT_PATH:src/main/resources/firebase-service-account.json}

# Gemini AI
gemini.api-key=${GEMINI_API_KEY:}
gemini.model=${GEMINI_MODEL:gemini-3.1-flash-lite}
gemini.base-url=https://generativelanguage.googleapis.com/v1beta

# Supabase
supabase.url=${SUPABASE_URL:https://supabase.com/dashboard/project/zqnpvghvempfcqrssisd}
supabase.secret-key=${SUPABASE_SECRET_KEY:}

# CORS
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,https://ai-mock-interview-preperation.netlify.app}

# JWT
jwt.secret=${JWT_SECRET:default_secret_change_me}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Logging
logging.level.com.aiinterview=DEBUG
logging.level.org.springframework.security=INFO
